name: tmp-runway-media-download
on:
  push:
    branches: [tmp-runway-media-bridge]
    paths:
      - 'tmp-runway-chunk-*.tsv'
      - '.github/workflows/tmp-runway-download.yml'
permissions:
  contents: read
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download Runway media
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runway-media
          cat tmp-runway-chunk-00.tsv tmp-runway-chunk-01.tsv tmp-runway-chunk-02.tsv tmp-runway-chunk-03.tsv > tmp-runway-all.tsv
          test "$(wc -l < tmp-runway-all.tsv)" -eq 88
          while IFS=$'\t' read -r name url; do
            [ -n "$name" ] || continue
            echo "Downloading $name"
            curl --fail --location --retry 3 --silent --show-error "$url" -o "runway-media/$name"
            test -s "runway-media/$name"
            file "runway-media/$name" | grep -Eiq 'JPEG|image data'
          done < tmp-runway-all.tsv
          test "$(find runway-media -maxdepth 1 -type f | wc -l)" -eq 88
      - name: Upload media artifact
        uses: actions/upload-artifact@v4
        with:
          name: runway-media-88
          path: runway-media/
          if-no-files-found: error
          retention-days: 1
