name: tmp-runway-media-download
on:
  push:
    branches: [tmp-runway-media-bridge]
    paths:
      - 'tmp-runway-corrections.tsv'
      - '.github/workflows/tmp-runway-download.yml'
permissions:
  contents: read
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download accepted correction media
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runway-media
          test "$(wc -l < tmp-runway-corrections.tsv)" -eq 6
          while IFS=$'\t' read -r name url; do
            [ -n "$name" ] || continue
            echo "Downloading $name"
            curl --fail --location --retry 3 --silent --show-error "$url" -o "runway-media/$name"
            test -s "runway-media/$name"
            file "runway-media/$name" | grep -Eiq 'JPEG|image data'
          done < tmp-runway-corrections.tsv
          test "$(find runway-media -maxdepth 1 -type f | wc -l)" -eq 6
      - name: Upload media artifact
        uses: actions/upload-artifact@v4
        with:
          name: runway-corrections-6
          path: runway-media/
          if-no-files-found: error
          retention-days: 1
